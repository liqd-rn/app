import { StyleSheet } from 'react-native';

const Style = StyleSheet.create(
{
    wrapper:
    {
        position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', backgroundColor: 'white',
    }
});

export default Style;